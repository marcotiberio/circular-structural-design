<div class="flyntComponent componentSpacing {{ options.theme }}" is="flynt-block-wysiwyg2" style="background-color: {{ bgColor|e }};">
  <div class="container centerMaxWidthContainer">
    {% if header %}
      <h1 class="preContent">{{ header|e }}</h1>
    {% endif %}
    <div class="content content--{{ textAlignment }} centerContentMaxWidth">
      <div class="col">{{ contentHtmlSx|e('wp_kses_post') }}</div>
      <div class="col">{{ contentHtmlDx|e('wp_kses_post') }}</div>
    </div>
  </div>
</div>
